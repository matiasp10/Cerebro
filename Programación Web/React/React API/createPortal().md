```
createPortal(child, container)
```

Crea un portal. Los portales proporcionan una forma de [renderizar hijos en un nodo del DOM que existe fuera de la jerarquía del DOM del componente](https://es.reactjs.org/docs/portals.html).
